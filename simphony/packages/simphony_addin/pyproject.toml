[project]
name = "simphony_addin"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Alexandra Watkins", email = "xandra.watkins@gmail.com" }
]
requires-python = ">=3.13"
dependencies = ["simphony_core"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
sources = ["src"]
only-include = ["src/simphony/addin"]
# packages = [
#     "src/simphony"
#     ]